(function () {
    'use strict';

    app.controller('memberController', memberController);

    memberController.$inject = ['$scope', '$rootScope', '$filter', '$window', '$location', 'localStorageService', 'memberFactory'];

    function memberController($scope, $rootScope, $filter, $window, $location, localStorageService, memberFactory) {
      
    }
})();
